static const float mLightIntensityMin = 1.0;
static const float mLightIntensityMax = 10.0;
static const float mLightDistance = 1000;
static const float mLightPlaneNear = 0.1;
static const float mLightPlaneFar = 400.0;
static const float mTemperature = 6600.0; // range 1000 ~ 40000 // Default value D66 used for pure white
static const float mEnvLightIntensityMin = 1.0;
static const float mEnvLightIntensityMax = 6.2831852;
static const float mBloomIntensityMin = 1.0;
static const float mBloomIntensityMax = 20.0;
static const float mBloomGhostThresholdMax = 10.0;
static const float mExposureMin = 2.0;
static const float mExposureMax = 8.0;
static const float mExposureEyeAdapationMin = 0.0;
static const float mExposureEyeAdapationMax = 8.0;
static const float mVignetteInner = 1.0;
static const float mVignetteOuter = 3.5;
static const float mPointLightNear = 1.0;
static const float mPointLightFar  = 400.0;
static const float mPSSMCascadeZMin = 5;
static const float mPSSMCascadeZMax = 1500;
static const float mPSSMCascadeLambda = 0.5;
static const float mPSSMDepthZMin = 0;
static const float mPSSMDepthZMax = 4000.0;
static const float mSunLightSize = 0.035;              // Sun virtual size / angular diameter (penumbra spread rate)
static const float mSunPCSSBlockerSearchScale = 1.0;   // Blocker search radius multiplier
static const float mSunPCSSMinFilterRadius = 1.5;      // Minimum PCF filter radius in texels (sharp anti-aliased contact)
static const float mSunPCSSMaxFilterRadius = 28.0;     // Maximum PCF filter radius in texels (prevents noise in large penumbras)
static const float mSSRRangeMax = 1000.0;
static const float mSSRRangeScale = 0.75;
static const float mSSRThreshold = 1.0;
static const float mSSRFadeStart = 0.8;
static const float mGISaturation = 1.0;
static const float mGIBlurSharpness = 2.0;
static const float mSSGIScale = 1.5;
static const float mSSGIIntensityDefault = 1.2;
static const float mSSGIBiasDefault = 0.2;
static const float mSSGIConeAngleDefault = 0.5;
static const float mSSGIThicknessWeight = 0.35;
// Upstream order: the untouched controller sits at Min (moderate scatter),
// SSSS+ lowers the scale toward Max (wider kernel), SSSS- relaxes it to 0.25
// (near-off) in ray.fx.  Swapping these makes the default scene over-scattered.
static const float mSSSSIntensityMin = 0.04;
static const float mSSSSIntensityMax = 0.02;
static const float mSSDOParams[4] = {2.0, 2.0, 0.03, 0.15};
static const float mSSDOBiasNear = 0.125;
static const float mSSDOBiasFar = 0.0005;
static const float mSSDOBiasFalloffNear = 20.0;
static const float mSSDOIntensityMin = 2.4;
static const float mSSDOIntensityMax = 10.0;
static const float mSSDOBlurFalloff = 200.0;
static const float mSSDOBlurSharpnessMin = 1.0;
static const float mSSDOBlurSharpnessMax = 8.0;
static const float mAmbientOcclusionMaxRadiusPixelPercentage = 0.1;
static const float mAmbientOcclusionBilateralSharpness = 8.0;
static const float mAmbientOcclusionBias = 0.2;
static const float mFXAAQualitySubpix = 0.5;
static const float mFXAAQualityEdgeThreshold = 0.166;
static const float mFXAAQualityEdgeThresholdMin = 0.0333;
static const float mSSRBlurMin = 512;
static const float mSSRBlurMax = 4096;
static const float mSSRBlurDefault = 1024;
static const float mSSRBrightnessMin = 0.0;
static const float mSSRBrightnessMax = 10.0;
static const float mSSRBrightnessDefault = 3.0;
static const float mSSROffsetMin = 0.05;
static const float mSSROffsetMax = 2.0;
static const float mSSROffsetDefault = 0.1;
static const float mSSRThicknessMin = 0.01;
static const float mSSRThicknessMax = 5.0;
static const float mSSRThicknessDefault = 0.5;
static const float mSSRFresnelMin = 0.0;
static const float mSSRFresnelMax = 1.0;
static const float mSSRFresnelDefault = 0.6;
static const float mSSRSmoothnessMin = -1.0;
static const float mSSRSmoothnessMax = 2.0;
static const float mSSRSmoothnessDefault = 2.0;

static const float mSensorWidth = 36.0f; // 35mm "full-frame" film format is 36mm x 24mm

// Top-Down Sky Visibility / Heightfield Ambient Occlusion
#define SKY_VISIBILITY_MAP_SIZE 1024
static const float mSkyVisibilityRange = 160.0;        // Orthographic horizontal coverage in world units (160m x 160m)
static const float mSkyVisibilityHeightMin = -100.0;   // Minimum world Y bound
static const float mSkyVisibilityHeightMax = 300.0;    // Maximum world Y bound
static const float mSkyVisibilityMaxHeight = 80.0;     // Maximum occluder height threshold above receiver
static const float mSkyVisibilityIntensity = 1.0;      // Sky occlusion strength
static const float mSkyVisibilityMinFloor = 0.05;      // Ambient floor under deepest roofs (prevents pitch black)
